# Nifki

To run the compiler from the command line:

```
java -cp sss.jar:compiler/compiler.jar org.sc3d.apt.crazon.compiler.Compiler ../wiki/ Rocks
```

To run the VM from the command line:

```
java -cp ../wiki/nifki-out/Rocks.jar:vm/nifki-lib.jar org.sc3d.apt.crazon.vm.Main 512 512 30
```
